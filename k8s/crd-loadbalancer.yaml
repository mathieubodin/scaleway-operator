apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: loadbalancers.scaleway.mathieubodin.io
spec:
  group: scaleway.mathieubodin.io
  names:
    categories: []
    kind: LoadBalancer
    plural: loadbalancers
    shortNames: []
    singular: loadbalancer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.scalewayId
      name: Scaleway ID
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.vipAddress
      name: VIP
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for LoadBalancerSpec via `CustomResource`
        properties:
          spec:
            properties:
              description:
                description: Description optionnelle
                nullable: true
                type: string
              lb_type:
                description: Type de load balancer (LB-S, LB-GP, etc.)
                type: string
              name:
                description: Nom du load balancer Scaleway
                type: string
              tags:
                default: []
                description: Tags optionnels
                items:
                  type: string
                type: array
              zone:
                description: Zone Scaleway (fr-par-1, nl-ams-1, etc.)
                type: string
            required:
            - lb_type
            - name
            - zone
            type: object
          status:
            nullable: true
            properties:
              created_at:
                description: Timestamp de création
                format: date-time
                nullable: true
                type: string
              error_message:
                description: Message d'erreur si applicable
                nullable: true
                type: string
              project_id:
                description: ID du projet Scaleway (pour tracking)
                nullable: true
                type: string
              scaleway_id:
                description: ID Scaleway du load balancer
                nullable: true
                type: string
              state:
                default: ''
                description: État actuel (pending, ready, error, etc.)
                type: string
              sync_state:
                default: ''
                description: État de la synchronisation (Pending, Synced, Syncing, Error, TerminationBlocked, etc.)
                type: string
              vip_address:
                description: Adresse VIP (IP publique du LB)
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: LoadBalancer
        type: object
    served: true
    storage: true
    subresources:
      status: {}
