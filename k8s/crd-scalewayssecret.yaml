apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: scalewaysecrets.scaleway.mathieubodin.io
spec:
  group: scaleway.mathieubodin.io
  names:
    categories: []
    kind: ScalewaySecret
    plural: scalewaysecrets
    shortNames: []
    singular: scalewaysecret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.scalewayId
      name: Scaleway ID
      type: string
    - jsonPath: .status.currentVersion
      name: Version
      type: integer
    - jsonPath: .status.syncState
      name: State
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ScalewaySecretSpec via `CustomResource`
        properties:
          spec:
            properties:
              description:
                description: Description optionnelle
                nullable: true
                type: string
              name:
                description: Nom du secret dans Scaleway Secret Manager
                type: string
              region:
                description: Région Scaleway (fr-par, nl-ams, pl-waw)
                type: string
              source:
                description: Source de la valeur à synchroniser
                properties:
                  kubernetes_secret:
                    description: Lit la valeur depuis un Secret Kubernetes du même namespace
                    nullable: true
                    properties:
                      key:
                        description: Clé dans `.data` du Secret
                        type: string
                      name:
                        description: Nom du Secret Kubernetes
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              tags:
                default: []
                description: Tags supplémentaires (en plus des tags operator injectés à la création)
                items:
                  type: string
                type: array
            required:
            - name
            - region
            - source
            type: object
          status:
            nullable: true
            properties:
              current_version:
                description: Numéro de révision de la version active
                format: uint32
                minimum: 0.0
                nullable: true
                type: integer
              error_message:
                description: Message d'erreur si applicable
                nullable: true
                type: string
              last_synced_resource_version:
                description: |-
                  resourceVersion du Secret K8s source à la dernière synchronisation.
                  Sert à détecter une rotation sans exposer de dérivée de la valeur.
                nullable: true
                type: string
              scaleway_id:
                description: ID Scaleway du secret
                nullable: true
                type: string
              sync_state:
                default: ''
                description: État de la synchronisation (Synced, Syncing, Error)
                type: string
            type: object
        required:
        - spec
        title: ScalewaySecret
        type: object
    served: true
    storage: true
    subresources:
      status: {}
